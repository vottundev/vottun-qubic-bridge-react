// import { useTranslation } from "react-i18next";
import type { FunctionComponent } from "@/common/types";

export const Policy = (): FunctionComponent => {
    return <div>This is the Policy page</div>;
};
